# ============================================================================
# AUTOGNOSTIC PLUGIN CONFIGURATION
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------------------------------------------

# Option 1: Local development with PGlite (embedded PostgreSQL)
# This is the default for ElizaOS - no DATABASE_URL needed
# Tables are created automatically from the plugin schema export
# PGLITE_DATA_DIR=./.eliza/.elizadb

# Option 2: Cloud deployment with external PostgreSQL
# Uncomment and set your connection string:
# POSTGRES_URL=postgresql://user:password@host:5432/database

# ----------------------------------------------------------------------------
# AUTHENTICATION
# ----------------------------------------------------------------------------

# Auth token for write operations (adding/removing sources)
# If not set, authentication is disabled (open access)
AUTOGNOSTIC_AUTH_TOKEN=your-secure-token

# ----------------------------------------------------------------------------
# SCIENTIFIC PAPER DETECTION
# ----------------------------------------------------------------------------

# Crossref API email (optional, improves rate limits from 50/sec to higher)
# Without this, you may hit rate limits when ingesting many papers
CROSSREF_MAILTO=your@email.com

# ----------------------------------------------------------------------------
# SCHEDULED SYNC
# ----------------------------------------------------------------------------

# Cron expression for background sync (default: 3 AM daily)
# AUTOGNOSTIC_SYNC_CRON=0 3 * * *

# Timezone for sync schedule (default: UTC)
# AUTOGNOSTIC_SYNC_TIMEZONE=UTC

# Hours before a source is considered stale (default: 24)
# AUTOGNOSTIC_STALENESS_HOURS=24

# Enable/disable scheduled sync (default: true)
# AUTOGNOSTIC_SYNC_ENABLED=true

# ----------------------------------------------------------------------------
# GITHUB INTEGRATION (Optional - for repository discovery)
# ----------------------------------------------------------------------------

# GitHub personal access token for private repo access
# GITHUB_TOKEN=ghp_your_token_here
